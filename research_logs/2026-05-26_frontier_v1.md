# Frontier opponent v1 + probe matrix v1

**Date:** 2026-05-26  
**Status:** Frozen baseline — do not change `opponent_model.py` until 30-seed matrix is recorded.

## Hypothesis

Rollout regret was dominated by **passive enemy** → hybrid overestimated expansion (especially neutral capture). A **light frontier opponent** (reinforce threatened + punish exposed) makes rollout futures “push back” without full v2 opponent cost.

## Changes (frozen)

| Component | Version |
|-----------|---------|
| Rollout opponent | `src/search/opponent_model.py` frontier v1 (`ORBIT_ROLLOUT_OPPONENT=frontier`) |
| World step | B2 orbit + comet `path_index` in `simulate_one_turn` |
| Hybrid safety | `pick_legal_or_fallback` → v2 on illegal rollout pick |
| Probe | `scripts/probe_matrix.py`, `seeds_probe_v1.json` (30 seeds) |

## Probe matrix

```bash
PYTHONPATH=. .venv/bin/python scripts/probe_matrix.py
```

| Cell | Game | Rollout opponent |
|------|------|------------------|
| v2_vs_random | v2 vs random | — |
| hybrid_passive | hybrid vs v2 | passive |
| hybrid_frontier | hybrid vs v2 | frontier |

Artifacts:

- `probe_results/matrix_report_v1.json`
- `probe_results/regret_report_hybrid_frontier_v1.json`
- `probe_results/decision_delta_hybrid_frontier_v1.jsonl`

## Early smoke (2 seeds — directional only)

| Metric | passive | frontier |
|--------|---------|----------|
| hybrid_worse_rate | ~75% | ~25% |
| avg_ship_diff | ~-12 | ~-1.2 |

**Not conclusive** — run full 30 seeds.

## Metrics to read (in order)

1. **`regret.by_bucket`** — not overall winrate  
2. **`neutral_capture`** — still negative? → expansion still overvalued  
3. **`aggressive_expansion`** — ≈ 0 regret? → frontier punish works  
4. **`comet_chase`** — suspected remaining bias (fleet motion / intercept)  
5. **`decision_reversal_rate`** — diverged from v2 but real-env worse (“confidently wrong”)  
6. **`same_decision_rate`** — rollout aligning with v2 heuristics  

## Success criteria (Phase B2.5 complete)

- `hybrid_worse_ships_rate` < 55%
- `avg_ship_diff` ≈ 0
- `episode_invalid` = 0
- No bucket systematically negative at 30 seeds
- `decision_reversal_given_diverged` falling vs passive ablation

## Explicitly NOT doing yet

- Top-K scheduler / MCTS / value net / RL
- Heavier frontier (swarm, multi-move enemy)
- Scheduler until regret ≈ 0 on frozen matrix

## Next hypotheses (after 30-seed run)

1. If **`neutral_capture`** still negative → stronger punish or neutral-specific rollout penalty  
2. If **`comet_chase`** negative → comet ETA / fleet motion fidelity in rollout  
3. If regret ≈ 0 → **Phase B3**: rollout assists multi-move scheduling (not replacement planner)

## Decision delta gold data

`decision_delta_hybrid_frontier_v1.jsonl` — use for:

- Rollout bias postmortems  
- Future value / classifier training  
- Filtering “hybrid changed v2 and lost ships” cases (`decision_reversal: true`)
