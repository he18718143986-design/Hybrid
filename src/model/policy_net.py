"""Policy / ranking network stub."""

from __future__ import annotations

from typing import List


def predict_policy_logits(features: List[float], model=None) -> float:
    _ = (features, model)
    return 0.0
